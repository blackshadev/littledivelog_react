import styled from 'styled-components';

export const Nav = styled.nav`
    ul {
        list-style: none;
        margin-top: 1rem;
    }

    a {
        display: block;
        padding: 0.5rem 1rem;
        text-decoration: none;
        width: 100%;
        white-space: nowrap;
    }
`;
