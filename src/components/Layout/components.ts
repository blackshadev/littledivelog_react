import styled from 'styled-components';

export const Main = styled.div``;

export const Container = styled.div`
    display: flex;

    nav {
        width: 180px;
    }

    ${Main} {
        flex: 1;
    }
`;
